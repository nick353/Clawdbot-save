# 🎬 ffmpeg-video-processor

Sora動画の画質改善・音声処理を自動化するスキル

## クイックスタート

### 1. 完全処理（画質 + 音声）

```bash
cd /root/clawd/skills/ffmpeg-video-processor
./process-all.sh input.mp4 output.mp4
```

### 2. 画質改善のみ

```bash
./video-processor.sh improve input.mp4 output.mp4
```

### 3. 音声改善のみ

```bash
./audio-enhance.sh enhance input.mp4 output.mp4
```

### 2. プリセットを使う

```bash
# YouTube用（1080p, 5000k）
./video-processor.sh improve input.mp4 output.mp4 --preset youtube

# Instagram用（720p, 3500k）
./video-processor.sh improve input.mp4 output.mp4 --preset instagram

# X用（720p, 2500k）
./video-processor.sh improve input.mp4 output.mp4 --preset twitter
```

### 3. カスタム設定

```bash
./video-processor.sh custom input.mp4 output.mp4 \
  --denoise high \
  --sharpen medium \
  --bitrate 8000k
```

### 4. バッチ処理

```bash
./video-processor.sh batch /path/to/videos/
```

## 処理内容

### ノイズ除去
- **low**: 軽度のノイズ除去（速い）
- **medium**: 標準的なノイズ除去（推奨）
- **high**: 強力なノイズ除去（遅い）

### シャープ化
- **low**: 軽度のシャープ化
- **medium**: 標準的なシャープ化（推奨）
- **high**: 強力なシャープ化

### ビットレート
- **2500k**: 低画質（小ファイル）
- **5000k**: 標準（推奨）
- **8000k**: 高画質（大ファイル）

## ワークフロー

### 推奨フロー（完全処理）

1. **動画アップロード**
   - andoさんがSora動画をDiscordにアップロード

2. **完全処理（自動 + 半自動）**
   - リッキーが `process-all.sh` で処理
   - 画質改善（自動）
   - 音声改善（Adobe Podcast - 半自動）

3. **字幕追加（手動）**
   - andoさんが字幕を追加

4. **SNS投稿（自動）**
   - リッキーが `sns-multi-poster` で一括投稿

### 個別処理フロー

- **画質改善のみ**: `video-processor.sh improve`
- **音声改善のみ**: `audio-enhance.sh enhance`
- **音声抽出**: `audio-enhance.sh extract`
- **音声結合**: `audio-enhance.sh replace`

## 自動クリーンアップ

処理完了後、自動的にファイルを削除してディスク容量を節約します。

### 削除されるファイル
- **即座に削除**: 入力ファイル、一時ファイル
- **24時間後に削除**: 出力ファイル（Discord送信後）

### 手動クリーンアップ
```bash
./auto-cleanup.sh
```

### ディスク使用量確認
```bash
du -sh /root/clawd/video-processing
```

## トラブルシューティング

### エラー: ffmpeg not found
```bash
apt-get update && apt-get install -y ffmpeg
```

### 処理が遅い
- `--denoise low` を使用
- `--preset twitter` で解像度を下げる

### ファイルサイズが大きすぎる
- `--bitrate 2500k` でビットレートを下げる
- `--preset twitter` を使用

### ディスク容量不足
```bash
# 古いファイルを即座に削除
./auto-cleanup.sh
```

## サンプル

`examples/` ディレクトリにサンプルスクリプトがあります：
- `youtube-optimize.sh`: YouTube用最適化
- `sns-optimize.sh`: SNS用軽量化
- `batch-process.sh`: 複数動画の一括処理

---

**作成日**: 2026-02-14
**作成者**: リッキー 🐥
**バージョン**: 1.0.0
